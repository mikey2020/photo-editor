<!DOCTYPE html>
<html> 
	<head>    
	<title><%= title %></title>
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>  

	</head>
	<% include temps/header.ejs %>  
	<body>   
		<!--<script src ="javascripts/main.js"></script>-->
		
		
		<h1>Welcome to <%= title %> edit page </h1> 
		<div id = "photo">
			<img src = "images/image.jpg" id="image" height="500" width="500">
			<button id="edit">edit</button>
		</div>
		<script type="text/javascript">
			var canvas = convertImageToCanvas(document.querySelector('img'));
			var ctx = canvas.getContext('2d');
			ctx.filter = 'blur(50px)';

			function convertImageToCanvas(image) {
				var canvas = document.createElement("canvas");
				canvas.width = image.width;
				canvas.height = image.height;
				canvas.getContext("2d").drawImage(image,image.width,);

				return canvas;
			}


			

		</script>
		
		<form action="/upload" method="POST" enctype="multipart/form-data" >
            <input type='file' name="image">
            <br>
            <input type="submit"  value="submit" >
		</form>
		
		
				
	</body> 
</html>